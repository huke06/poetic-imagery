# 「诗象志」古诗词意象智能体项目完整开发方案
## 一、项目整体概述
### 1. 项目定位
基于上海图书馆开放数据（诗文库+古代艺术品图文库），以**古典诗词意象**为核心切口，打造集「意象知识图谱、诗画联动展示、演变脉络可视化、智能问答创作」于一体的轻量化古诗词文化智能体。
项目面向古诗文爱好者，通过数据可视化与AI交互，直观呈现一个词语在千年诗词中的情感承载与演变轨迹，同时打通古画与诗意的关联，实现“以象览诗、以画证意”的沉浸式体验。

### 2. 核心差异化亮点（竞赛核心竞争力）
1. **意象数据化呈现**：将文学概念“意象”转化为可检索、可统计、可可视化的结构化知识库，用数据讲清意象的兴衰演变与情感变迁
2. **诗画双向联动**：打通诗文库与古代艺术品库，为每个意象匹配对应古画，实现“见意象知画意，观古画品诗情”，区别于纯文字诗词项目
3. **轻量智能体能力**：基于自建意象知识库实现RAG问答与格律创作，AI回答全部锚定本地权威数据，避免大模型幻觉
4. **极简国风视觉体系**：现代审美重构传统国风，以留白、宣纸质感、低饱和传统色打造高级视觉体验，演示与传播效果突出

### 3. 项目核心Slogan
一字藏万象，一诗见千年

## 二、整体技术架构与选型
### 1. 技术栈总览
| 层级 | 技术选型 | 选型说明 |
|------|----------|----------|
| 前端展示层 | Vue 3 + Vite + Tailwind CSS + ECharts | 开发效率高，组件生态成熟，可视化开箱即用，适配AI辅助开发 |
| 后端服务层 | FastAPI + SQLAlchemy 2.0 + Pydantic | 轻量高效，自动生成接口文档，Python生态适配数据处理与大模型调用 |
| 数据存储层 | SQLite（开发/演示） + 可选PostgreSQL | 零配置单文件数据库，适配中小规模数据，后期可平滑升级 |
| AI能力层 | 主流大模型API（如豆包/通义千问） + 本地关键词检索 | 轻量RAG方案，无需向量数据库，开发量极小，效果可控 |
| 部署层 | 前端：Vercel/GitHub Pages；后端：轻量云服务器/Render | 低成本静态托管+后端服务，无需复杂运维 |

### 2. 分层架构设计
```
┌─────────────────────────────────┐
│         前端展示层               │
│  意象画廊 / 详情页 / 古画展厅    │
│  智能助手 / 诗文详情 / 关于页    │
└─────────────┬───────────────────┘
              │
┌─────────────▼───────────────────┐
│         后端服务层               │
│  意象接口 / 诗文接口 / 书画接口  │
│  智能问答 / 管理后台 / 缓存层    │
└─────────────┬───────────────────┘
              │
┌─────────────▼───────────────────┐
│         数据层                   │
│  意象知识库 / 诗文库 / 书画库    │
│  关联关系表 / 标注数据           │
└─────────────┬───────────────────┘
              │
┌─────────────▼───────────────────┐
│      原始开放数据层              │
│  上图诗文库API / 上图艺术品API   │
└─────────────────────────────────┘
```

## 三、数据库设计
共设计7张核心表，覆盖全部业务需求，支持意象、诗文、书画、关联关系的完整存储与查询。

### 1. 意象主表 `concept`
存储意象的核心基础信息，为知识库核心。
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | int 主键 | 自增ID |
| name | varchar(32) 唯一 | 意象名称 |
| category | varchar(32) | 分类：天象/植物/动物/器物/地理 |
| original_meaning | text | 本义解释 |
| poetic_meaning | text | 诗词引申义与文化内涵 |
| emotion_tags | varchar(255) | 情感标签，逗号分隔 |
| origin_dynasty | varchar(32) | 起源朝代 |
| peak_dynasty | varchar(32) | 鼎盛朝代 |
| description | text | 详细阐释与演变说明 |
| create_time | datetime | 创建时间 |
| update_time | datetime | 更新时间 |

### 2. 诗文表 `poetry`
存储清洗后的诗文数据，来源为诗文库API。
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | int 主键 | 自增ID |
| source_writing_id | varchar(64) | 诗文库原始writingId |
| title | varchar(255) | 作品标题 |
| author | varchar(64) | 作者 |
| dynasty | varchar(32) | 朝代 |
| writing_type | varchar(32) | 体裁：诗/词/文 |
| content | text | 全文内容 |
| clauses | text | 分句内容，JSON数组存储 |
| create_time | datetime | 入库时间 |

### 3. 意象-诗文关联表 `concept_poetry_rel`
建立意象与诗句的多对多映射，支持名句检索。
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | int 主键 | 自增ID |
| concept_id | int 外键 | 关联意象ID |
| poetry_id | int 外键 | 关联诗文ID |
| clause | varchar(255) | 包含该意象的具体诗句 |
| weight | int | 关联权重（名句优先级） |
| is_classic | tinyint | 是否为经典名句 |

### 4. 艺术品表 `artwork`
存储古代书画数据，来源为艺术品图文库API。
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | int 主键 | 自增ID |
| source_work_id | varchar(64) | 艺术品库原始ID |
| name | varchar(255) | 作品名称 |
| artist | varchar(64) | 作者 |
| dynasty | varchar(32) | 朝代 |
| material | varchar(128) | 材质（如纸本设色） |
| size | varchar(64) | 尺寸 |
| subject_names | varchar(255) | 主题分类 |
| image_url | varchar(512) | 高清图片地址 |
| thumb_url | varchar(512) | 缩略图地址 |
| description | text | 作品简介 |

### 5. 意象-艺术品关联表 `concept_artwork_rel`
实现意象与古画的多对多匹配，支撑诗画联动。
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | int 主键 | 自增ID |
| concept_id | int 外键 | 关联意象ID |
| artwork_id | int 外键 | 关联艺术品ID |
| relation_desc | varchar(255) | 关联阐释（标注内容） |
| weight | int | 匹配优先级 |

### 6. 意象关联表 `concept_relation`
构建意象知识网络，支撑关联查询与图谱展示。
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | int 主键 | 自增ID |
| from_concept_id | int 外键 | 源意象ID |
| to_concept_id | int 外键 | 目标意象ID |
| relation_type | varchar(32) | 关系类型：对仗/共现/情感同源/演变衍生 |
| description | varchar(255) | 关系说明 |

### 7. 朝代统计表 `dynasty_stats`
预计算每个意象在各朝代的出现频次，直接供前端可视化调用，避免实时计算。
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | int 主键 | 自增ID |
| concept_id | int 外键 | 意象ID |
| dynasty | varchar(32) | 朝代 |
| count | int | 出现作品数量 |

## 四、后端服务完整设计（FastAPI）
### 1. 模块划分
后端按功能划分为5个模块，代码结构清晰，开发并行度高。
- `api/`：接口路由层，分意象、诗文、书画、智能问答、管理5个子路由
- `service/`：业务逻辑层，封装核心业务与数据处理
- `models/`：数据库模型定义（SQLAlchemy）
- `schemas/`：请求/响应数据模型（Pydantic）
- `scripts/`：数据爬取、清洗、统计脚本
- `utils/`：工具类（大模型调用、API请求、缓存等）

### 2. 核心接口清单
#### （1）意象模块
| 接口 | 方法 | 功能说明 |
|------|------|----------|
| `/api/concept/list` | GET | 获取意象列表，支持分类筛选、关键词搜索 |
| `/api/concept/{id}` | GET | 获取意象详情（聚合基础信息+朝代统计+情感标签） |
| `/api/concept/{id}/poetries` | GET | 获取意象关联的诗文名句列表，支持分页 |
| `/api/concept/{id}/artworks` | GET | 获取意象关联的古画列表 |
| `/api/concept/{id}/relations` | GET | 获取意象的关联意象网络 |

#### （2）诗文模块
| 接口 | 方法 | 功能说明 |
|------|------|----------|
| `/api/poetry/{id}` | GET | 获取诗文全文详情 |
| `/api/poetry/{id}/similar` | GET | 透传相似句子接口 |
| `/api/poetry/search` | POST | 组合搜索诗文（透传并缓存结果） |

#### （3）艺术品模块
| 接口 | 方法 | 功能说明 |
|------|------|----------|
| `/api/artwork/{id}` | GET | 获取艺术品详情 |
| `/api/artwork/list` | GET | 艺术品列表，支持朝代、主题筛选 |

#### （4）智能问答模块
| 接口 | 方法 | 功能说明 |
|------|------|----------|
| `/api/agent/ask` | POST | 智能问答，输入问题返回基于知识库的回答 |
| `/api/agent/compose` | POST | 意象创诗，输入意象+体裁，生成对应古诗 |

#### （5）管理后台模块（简易CRUD）
| 接口 | 方法 | 功能说明 |
|------|------|----------|
| `/api/admin/concept` | POST/PUT/DELETE | 意象数据增删改 |
| `/api/admin/relation` | POST/DELETE | 关联关系维护 |

### 3. 关键后端能力实现
#### （1）开放API透传与缓存机制
- 所有上游诗文库、艺术品库接口均通过后端代理调用，前端不直接请求第三方
- 对详情、平仄、笺注等不变数据做本地缓存（数据库+内存缓存），减少上游依赖，提升响应速度
- 统一异常处理，上游接口异常时返回本地兜底数据

#### （2）轻量RAG问答实现逻辑
1. 用户输入问题 → 后端提取核心关键词（意象名）
2. 本地SQL检索：匹配意象基础信息、关联名句、关联意象、对应古画
3. 拼装Prompt：将检索到的结构化数据作为上下文，约束大模型仅基于给定资料回答
4. 调用大模型API生成自然语言回答，同时返回引用的诗文与古画来源
5. 相同问题做结果缓存，降低调用成本

#### （3）意象创诗实现逻辑
1. 用户输入2-5个意象、指定体裁（五言绝句/七言绝句/律诗）
2. 后端提取对应意象的情感基调与经典语境，拼装创作提示词
3. 调用大模型生成符合格律的诗作
4. 可选调用平仄接口做简单格律校验，返回结果附带平仄标注

## 五、前端页面与交互完整设计
共设计6个核心页面，遵循「极简现代国风」视觉规范，信息层级清晰，交互流畅克制。

### 视觉设计基础规范
- **配色体系**：背景宣纸米白(#F5F1E8)，主色石青(#2B4C7E)，辅助色赭石(#9B4423)、竹青(#5B7C5F)，文字深灰(#2C2C2C)，全程低饱和
- **字体方案**：标题用思源宋体，正文用思源黑体，诗句单独用思源宋体大号排版
- **动效原则**：全量淡入淡出过渡、卡片hover微上浮、图表渐入加载，无花哨动效
- **装饰元素**：极细线条云纹分隔、淡墨纹理背景、边角印章元素，点到为止

### 1. 首页 / 门户页
**页面定位**：项目入口，营造氛围，引导核心功能
- **首屏区**：居中项目名称+Slogan，背景为淡墨山水底纹，下方设两个核心入口按钮：「意象漫游」「古画寻诗」
- **意象精选区**：横向滚动展示8个高频经典意象卡片，点击直达详情
- **项目简介区**：三栏布局，分别介绍「意象演变」「诗画互证」「智能问答」三大核心价值
- **页脚**：标注数据来源（上海图书馆开放数据竞赛）、团队信息

### 2. 意象画廊页
**页面定位**：意象知识库总览，支持浏览与检索
- **顶部筛选栏**：分类标签（天象/植物/动物/器物/地理）+ 搜索框，支持按名称快速检索
- **卡片网格区**：响应式瀑布流布局，每个意象为一张卡片
  - 卡片内容：意象名称、一句经典名句、对应主题色底色、右下角小尺寸关联古画缩略图
  - 交互：hover轻微上浮+阴影加深，点击进入意象详情页
- **底部分页**：简单分页加载，无需无限滚动

### 3. 意象详情页（项目核心页面）
单页纵向分6个区块，完整呈现一个意象的全部维度，信息密度高且逻辑递进。
1. **头部概览区**
   - 左侧：大号意象名称 + 一句话核心定义 + 情感标签组
   - 右侧：环形情感分布图（ECharts），直观展示思乡、高洁、离愁等情感占比
2. **演变脉络区**
   - 横向朝代时间轴 + 频次折线图（ECharts），标注先秦、汉魏、唐、宋、元明清各节点
   - 点击对应朝代，下方名句列表自动筛选该朝代作品
3. **经典名句区**
   - 按朝代/优先级排序的诗句列表，每条显示诗句、作者、篇目
   - 点击诗句跳转全诗详情页，标注“经典名句”标识
4. **对仗与意象关联区**
   - 左侧：高频对仗词组+对应律句（调用对仗接口）
   - 右侧：轻量力导向关系图，展示关联意象，点击可跳转对应意象详情
5. **诗画相映区**
   - 横向卡片展示3-5幅对应古画缩略图，配一句关联阐释
   - 点击古画弹出大图预览，或跳转艺术品详情页
6. **扩展工具区**
   - 快捷按钮：查看同韵脚作品、查看相似句子，跳转对应结果

### 4. 古画展厅页
**页面定位**：独立的古代艺术品浏览入口，体现图文库价值
- **筛选栏**：按朝代、主题分类筛选
- **画廊网格**：瀑布流展示古画缩略图，标注作品名、作者、朝代
- **点击弹窗**：点击弹出大图，侧边展示作品详情（材质、尺寸、简介），底部显示“相关意象”标签，点击跳转对应意象详情

### 5. 智能助手页
**页面定位**：Agent交互入口，对话式体验
- **左侧对话区**：聊天式交互界面，用户输入问题，AI返回回答
- **右侧快捷提问区**：预设高频问题按钮，如“月亮在古诗里有什么含义？”“梅花和兰花有什么关联？”“用‘柳’和‘雁’写一首七言绝句”
- **回答样式**：回答包含文字解释 + 引用的名句 + 关联古画缩略图 + 相关意象跳转链接
- 创诗结果单独排版，支持一键复制

### 6. 诗文详情页
**页面定位**：全诗沉浸式阅读
- 居中古籍式排版，标题、作者、朝代、正文逐行排列，思源宋体大字号
- 底部工具栏：平仄标注、自动笺注、古籍出处、相似作品四个切换按钮
- 点击诗中高亮的意象词，可跳转对应意象详情页

## 九、优化建议
### 1.优化项目
1. 突出数据再加工价值：详细说明如何基于原始开放数据构建结构化意象知识库
2. 强调文化传播价值：面向大众的科普属性，降低古诗文欣赏门槛
3. 移动端适配：简单响应式适配，手机端也可流畅浏览，体现普适性
4. 增加分享功能：一键生成意象卡片图片，便于社交传播，体现项目影响力

